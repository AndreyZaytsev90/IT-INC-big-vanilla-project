import React from 'react';
import './App.css';


function App() {
 /* const names = [ "Andrey", "Olga", "Petr"]
  const users = [ {id: 1, name: "Andrey"}, {id: 2, name: "Olga"}, {id: 2, name: "Petr"}]
  const lielements = names.map(name => <li>{name}</li>)

  const lielementsUsers = users.map((user, index) => <li key={user.id}>{user.name}</li>)*/


  return (
    <div className="App">
     {/* <ul>
        {lielements}
        {lielementsUsers}
      </ul>*/}

    </div>
  );
}

export default App;
